apiVersion: v1
kind: Pod
metadata:
  name: ifremer-sync-redo-patch
  labels:
    tier: api
spec:
  volumes:
    - name: argovis-bulk
      persistentVolumeClaim:
        claimName: argovis-bulk
    - name: logs
      persistentVolumeClaim:
        claimName: logs
  containers:
  - name: sync
    imagePullPolicy: Always
    image: argovis/ifremer-sync:dev
    command: ['bash', 'ifremer-sync-redo.sh', '/logs/ifremer/20260407-repair-bgc']
    volumeMounts:
      - mountPath: "/bulk"
        name: argovis-bulk
      - mountPath: "/logs"
        name: logs
    resources:
      requests:
        memory: "0Gi"
        cpu: "0m"
      limits:
        memory: 2Gi
        cpu: 1000m
  restartPolicy: Never
